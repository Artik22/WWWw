@using web.Models
@model IEnumerable<web.Models.User> 


@{
    ViewData["Title"] = "Test";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>

    <div>
        
      
        @Html.DisplayNameFor(model => model.FirsName)
    </div>

    <div>
        @Html.DisplayNameFor(model => model.LastName)
    </div>
    <ul>
        @*@foreach (Article item in Model.Articles)
        {
            <li>@item.Title </li>
            @Html.ListBoxFor(x => item.Id , item.Users , new { style = "width:200px" })
        }*@
    </ul>

</div>




@*<div class="container">

        <script src="~/ckeditor/ckeditor.js"></script>
        <div>
            @using (Html.BeginForm("Index", "MyTest", FormMethod.Post))
            {
            <div>
                <p>
                    <label>Имя</label><br />
                    @Html.TextBox("Name")
                </p>
            </div>

                <div>
                    <textarea id="content1" name="content1">

    </textarea>
                </div>
                <div>
                    <input type="submit" value="Отправить" />
                </div>
            }
        </div>
    </div>*@

@*<div class="row">
        <div>
            @if (ViewBag.Content1 != default(string))
            {
                @Html.Raw(ViewBag.Content1)
            }
        </div>
    </div>*@
<script>
    document.addEventListener("DOMContentLoaded", function (event) {

        var editor = CKEDITOR.replace('content1');
    });
</script>